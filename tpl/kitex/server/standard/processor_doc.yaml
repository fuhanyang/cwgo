path: biz/processor/doc.go
update_behavior:
  type: skip
body: |-
  // Package processor is where your core business processor should be implemented.
  //
  // Recommended structure:
  // - Keep processor stateless and atomic (Operators).
  // - Use the 'strategy' package to orchestrate these operators.
  // - You can organize files by domain, feature, or service as you see fit.
  package processor

