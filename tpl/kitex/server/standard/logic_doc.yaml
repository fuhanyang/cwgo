path: biz/logic/doc.go
update_behavior:
  type: skip
body: |-
  // Package logic is where your core business logic should be implemented.
  //
  // Recommended structure:
  // - Keep logic stateless and atomic (Operators).
  // - Use the 'strategy' package to orchestrate these operators.
  // - You can organize files by domain, feature, or service as you see fit.
  package logic

